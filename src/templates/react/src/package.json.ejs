{
  "name": "<%= projectName %>",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"<% if (features.includes('router')) { %>,
    "react-router-dom": "^6.0.0"<% } %>
  },
  "devDependencies": {
    "vite": "^4.0.0"<% if (features.includes('typescript')) { %>,
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0"<% } %>
  }
}